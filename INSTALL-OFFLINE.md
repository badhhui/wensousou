# 文搜搜离线安装说明

适用目标：UOS 专业版 1022、ARM64、glibc 2.28。

## 安装

将整个 `offline-kit` 目录复制到离线电脑，进入目录后执行：

```bash
chmod +x check-target.sh
./check-target.sh wensousou_1.0.18_arm64.deb
sudo dpkg -i wensousou_1.0.18_arm64.deb
wensousou --self-check
```

预检通过后，可以从桌面应用菜单启动“文搜搜”，也可以在终端执行：

```bash
wensousou
```

如果桌面启动或界面显示仍有异常，请在终端执行：

```bash
wensousou-diagnose
```

该命令会检查 XCB、字体渲染、Qt DBus、Fcitx 输入法插件依赖和运行日志。

如果目录已经完成更新但仍然搜索不到内容，可附带一个应当命中的关键词：

```bash
wensousou-diagnose 党费
```

诊断结果会列出索引目录、成功文档、失败文档、空正文文档和该关键词的命中数量。

## 数据位置

- 索引数据库：`~/.local/share/wensousou/index.db`
- 状态目录：`~/.local/state/wensousou/`

卸载应用不会自动删除用户索引。如需彻底清理，可在卸载后手动删除索引数据库。

从旧版本升级后，首次启动会保留目录配置并自动清除旧文档索引，随后在后台重新建立
索引。`1.0.9` 在内置 Qt 中启用 DBus，并随应用携带 Fcitx Qt5 输入法插件，用于修复
UOS Fcitx 4 环境中搜索框无法调起中文输入法的问题。

`1.0.18` 修复搜索结果文件名列重影问题，并将“大小”列调整为“文件大小”。

`1.0.17` 优化搜索结果列宽和命中摘要展示，文件类型筛选改为多选并在当前结果内快速
过滤；默认不再索引 TXT，可在设置中重新开启。

`1.0.16` 将索引管理中的目录删除移入后台线程，删除大索引时保持窗口响应，并显示
删除中的状态提示。

`1.0.15` 根据屏幕可用区域自动设置默认窗口大小，并精简首页顶部索引操作区，只保留
“索引管理”和“设置”。

`1.0.14` 将界面比例整体放大约 30%，扩大操作图标和命中摘要；目录统计持久化到
数据库，索引管理页面不再在每次打开时扫描全部文档。

`1.0.13` 将操作图标改为 Qt 内存绘制，不再依赖 SVG 图像插件，修复部分 UOS 环境中
预览、打开和文件夹按钮显示为空白的问题。

`1.0.12` 将索引管理统计移至后台线程，增加文件名高亮、固定 SVG 操作图标、结果区
排版优化和主窗口居中显示。

`1.0.11` 保留索引管理页面和搜索改进，恢复紧凑界面；启动器仅加载随包 Qt 插件，
避免系统主题插件改变控件样式，同时继续使用随包 Fcitx 输入法插件。

`1.0.10` 将目录维护移入“索引管理”页面，增加失败文件原因列表和目录统计；首页突出
搜索框并支持折叠搜索区。启动后的增量更新默认关闭，需要时可在“设置”中启用。

如果需要完全重置目录配置，也可以在启动文搜搜前手动删除旧数据：

```bash
rm -f ~/.local/share/wensousou/index.db*
rm -f ~/.config/WenSouSou/wensousou.conf
```

该操作只删除本地索引和旧版索引阈值设置，不会删除原始文档。

## 在断网机器重新编译

如果机器上保留了完整源码、`third_party/cache/` 和编译环境，可以完全离线重新打包：

```bash
cd ~/Documents/文搜搜
chmod +x scripts/*.sh parser/build.sh packaging/*.sh
./scripts/build-offline-local.sh
```

新安装包生成在：

```text
dist/offline-kit/wensousou_1.0.18_arm64.deb
```
